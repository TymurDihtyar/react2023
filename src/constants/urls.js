const baseURLCars = 'http://owu.linkpc.net/carsAPI/v1'
const baseURLCommentsPosts = 'https://jsonplaceholder.typicode.com'

const cars = '/cars'
const comments = '/comments'
const posts = '/posts'

const urls = {
    cars: {
        base:cars
    },
    comments: {
        base: comments
    },
    posts:{
        base: posts
    }
}

export {baseURLCars, baseURLCommentsPosts, urls}